// JavaScript source code
function inlineBlockDiff(){
    let ansblock = document.getElementById('solblock');
	ansblock.innerHTML = " Block elements always start in a new line and have top and bottom margins. It does not contain any other elements next to it.";
	let ansinline = document.getElementById('solinline');
	ansinline.innerHTML= " Inline elements occupy only enough width that is sufficient to it and allows other elements next to it which are inline. <br>Inline elements do not start from a new line and do not have top and bottom margins as block elements have."
	let ansimg.innerHTML = <p class="sol" id="solinlineblockimg"></p>£»
};