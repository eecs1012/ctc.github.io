// JavaScript source code
function soldom(){
	let soldom = document.getElementById("soldom");
	soldom.innerHTML = "html file maps to the document, element maps to the object<br>";
	soldom.innerHTML += "The DOM views an HTML document as a tree of nodes. A node represents an HTML element.";

}